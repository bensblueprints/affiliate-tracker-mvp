# Reflink configuration — copy to .env and edit

# Port the web app listens on
PORT=5350

# Admin password for the dashboard (change this!)
ADMIN_PASSWORD=change-me

# SQLite database location (marketing assets live next to it in ./data/assets)
DB_PATH=./data/reflink.db
